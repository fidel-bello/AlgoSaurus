import React from "react";
import styles from "./info.module.css";

const CountInfo = () => {
  return (
    <div className={styles.container}>
      <h2 className={styles.header}>Count Sort</h2>
    </div>
  );
};

export default CountInfo;
